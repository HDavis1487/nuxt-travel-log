name: Lint

on:
  pull_request:
    branches:
      - main

jobs:
  build:
  runs-on: ubuntu-latest

with:
  node-version: 20.x
steps:
  - uses: actions/checkout@v4
  - name: Use Node.js
uses: actions/setup-node@v4 npm install npm run lint
